export type Product = {
  id: string
  name: string
  categoryId: number
  image : string
  price : boolean
}
export type User = {
    firstName: string,
    lastName: string,
    email: string,
    Id: string,
    phoneNumber: number,
    role: string
}
